<%= link_to "ドメインのトップへ", autho_domain_path(:status => params['status'], :cn => params["cn"]) %>

<br><br>

作業状況：
<%= @status.status %>
<% case @status.status %>
<% when 0 %>
    未作成
<% when 1 %>
    作業中（作業者：<%= @status.creator.name unless @status.creator.blank? %>）
<% when 2 %> 
    作業完了（未確認）（作業者：<%= @status.creator.name unless @status.creator.blank? %>）
<% when 3 %>
    確認済み（作業者：<%= @status.creator.name unless @status.creator.blank? %>）（確認者：<%= @status.checker.name unless @status.checker.blank? %>）
<% end %>

<br><br>

<p style="color: red;"><%= params["msg"] %><p>
<%= form_tag autho_domain_update_path(:status => params['status'], :cn => params["cn"]) do %>
    <%= hidden_field_tag "id", @id %>
    <%= hidden_field_tag "old_collage", @collage %>
    <table>
        <tr>
            <th>ドメイン</th><th>大学名（正式名称）</th>
        </tr>
        <tr>
            <td><%= text_field_tag "dom", @domain %></td>
            <td><%= text_field_tag "collage", @collage %></td>
        </tr>
    </table>
    <%= submit_tag("編集") %>
<% end %>

<br><br>

<div style="float: left; width: 40vw;">
    <table>
        <% @prefecture.each do |p| %>
            <tr><td><%= link_to p.prefecture, autho_domain_select_path(:p => p.objectId, :status => params['status'], :cn => params["cn"])%></td></tr>
        <% end %>
    </table>

    <br><br>
    <%= link_to "都道府県を追加", autho_domain_select_path(:d => @id, :status => params['status'], :cn => params["cn"]) %>
</div>

<div style="float: left; width: 40vw;">
    <table>
        <% @shorten.each do |s| %>
            <tr><td><%= link_to s.shortenName, autho_domain_shorten_path(:d_id => @id ,:s_id => s.objectId, :status => params['status'], :cn => params["cn"])%></td></tr>
        <% end %>
    </table>

    <br><br>
    <%= link_to "略称を追加", autho_domain_shorten_path(:d_id => @id, :status => params['status'], :cn => params["cn"]) %>
</div>